# Read by frontend/serve.py at startup. Injected into index.html as
# window.API_URL so cross-origin fetches go to the local Docker API
# instead of the production Fly.io URL. Not used in production
# (GitHub Pages serves static files only — no env handling).
API_URL=http://localhost:8000
